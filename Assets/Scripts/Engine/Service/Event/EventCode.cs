namespace BEBE.Engine.Event
{
    public enum EventCode
    {
        ON_GAME_START,
        ON_SERVER_CONNECTED,
        PING_RPC,
        SEND_JOIN_REQUEST,
        ON_CLIENT_CONNECTED,
        ON_CLIENT_DISCONNECTED,
        PING,
        ON_SYNC_CMD,
        ON_RECV_INPUT,
        ON_JOIN_REQUEST_RECV,
        ENUM_COUNT
    }
}
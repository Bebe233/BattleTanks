namespace BEBE.Engine.Event
{
    public enum EventCode
    {
        ON_GAME_START,
        GET_CHANNEL_ID,
        PING_RPC,
        SEND_JOIN_IN_REQUEST,
        RCP_FROM_CLIENT,
        ON_CLIENT_DISCONNECTING,
        PING,
        ON_SYNC_CMD,
        ON_RECV_INPUT,
        JOIN_IN_REQUEST,
        ENUM_COUNT
    }
}
namespace BEBE.Engine.Event
{
    public enum EventCode
    {
        ON_SERVER_CONNECTED,
        PING_RPC,
        ON_CLIENT_CONNECTED,
        ON_CLIENT_DISCONNECTED,
        PING,
        ON_SYNC_CMD,
        ON_RECV_INPUT,
        ENUM_COUNT
    }
}